---
description: Full validation — lint + typecheck + typegen + unit + e2e (report-only)
allowed-tools: Bash(pnpm check)
disallowed-tools: Edit, Write, NotebookEdit
---

Run `pnpm check`. Report all failures with file paths and line numbers. Do not attempt to fix anything — just report.
