---
description: Run unit tests with vitest (report-only)
allowed-tools: Bash(pnpm test)
disallowed-tools: Edit, Write, NotebookEdit
---

Run `pnpm test`. Report failures with test name, file path, and the assertion that failed. Do not attempt to fix anything — just report.
