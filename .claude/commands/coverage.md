---
description: Run vitest unit coverage and report the summary (report-only)
allowed-tools: Bash(pnpm test:coverage)
disallowed-tools: Edit, Write, NotebookEdit
---

Run `pnpm test:coverage`. Report the coverage summary (statements / branches / functions / lines) and call out the lowest-covered files. Do not attempt to fix anything — just report.
