---
description: Fast validation — lint + typecheck + typegen (report-only)
allowed-tools: Bash(pnpm check:fast)
disallowed-tools: Edit, Write, NotebookEdit
---

Run `pnpm check:fast`. Report all errors with file paths and line numbers. Do not attempt to fix anything — just report.
