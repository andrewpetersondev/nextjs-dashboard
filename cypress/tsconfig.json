// biome-ignore-all assist/source/useSortedKeys: order matters in tsconfig files
{
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": ["../tsconfig.json"],
	"compilerOptions": {
		// Modules
		"baseUrl": "..",
		"module": "esnext",
		"moduleResolution": "bundler",
		"types": ["cypress", "node", "@testing-library/cypress", "cypress-axe"],
		// Language and Environment
		"lib": ["esnext", "dom"],
		"target": "esnext",
		// Silence TS5101 for deprecated options inherited at runtime (e.g. downlevelIteration)
		"ignoreDeprecations": "6.0"
	},
	"include": [
		"../cypress.config.ts",
		"./**/*.ts",
		"./**/*.tsx",
		"../database/**/*.ts"
	]
}
