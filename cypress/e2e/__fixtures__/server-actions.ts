/**
 * Common server actions constants and paths used across tests.
 */
export const SERVER_ACTIONS_PATTERN = "/_server-actions/**" as const;

/** Paths for admin and dashboard sections used in server-action tests. */
export const DASHBOARD_ITEMS_PATH = "/dashboard/items" as const;
export const DASHBOARD_USERS_PATH = "/dashboard/users" as const;
export const ADMIN_USERS_PATH = "/admin/users" as const;
